import { CenterPiece, SkewbState } from "../solver/skewbState";

function generateRandomNSCase() {
    const cornerPerm = [0, 1, 2, 3, 4, 5, 6, 7];
    const centerPerm = 
    const skewbState = new SkewbState(
    )
}